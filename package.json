{
  "name": "toolsy-scholarship-updater",
  "version": "1.0.0",
  "description": "Scholarship data updater for Toolsy using CareerOneStop API",
  "private": true,
  "scripts": {
    "update-scholarships": "tsx scripts/update-scholarships.ts",
    "build:scholarships:wikidata": "tsx scripts/fetch-wikidata-scholarships.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
